{
  "name": "smart-attendance-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && mv frontend/build build"
  }
}
